FROM node:20

WORKDIR /app

COPY package.json ./

# Clean install without lock file to avoid optional dependency bug
RUN rm -rf node_modules package-lock.json && \
    npm install --force

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]