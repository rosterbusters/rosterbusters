services:
  traefik:
    volumes:
      # Serve a Cloudflare Origin CA certificate to the proxied edge in production.
      - ./traefik/certs:/certs:ro
      - ./traefik/dynamic:/dynamic:ro
    command:
      - --providers.file.directory=/dynamic
      - --providers.file.watch=true
